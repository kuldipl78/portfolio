{
  "name": "kuldip-portfolio",
  "version": "1.0.0",
  "description": "Authenticated Personal Portfolio Website for Kuldip Lohare",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["portfolio", "react", "node", "authentication"],
  "author": "Kuldip Lohare",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}